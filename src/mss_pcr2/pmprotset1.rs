#[doc = "Register `PMPROTSET1` reader"]
pub type R = crate::R<Pmprotset1Spec>;
#[doc = "Register `PMPROTSET1` writer"]
pub type W = crate::W<Pmprotset1Spec>;
#[doc = "Field `PCS32_PROT_SET` reader - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs32ProtSetR = crate::BitReader;
#[doc = "Field `PCS32_PROT_SET` writer - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs32ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS33_PROT_SET` reader - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs33ProtSetR = crate::BitReader;
#[doc = "Field `PCS33_PROT_SET` writer - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs33ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS34_PROT_SET` reader - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs34ProtSetR = crate::BitReader;
#[doc = "Field `PCS34_PROT_SET` writer - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs34ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS35_PROT_SET` reader - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs35ProtSetR = crate::BitReader;
#[doc = "Field `PCS35_PROT_SET` writer - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs35ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS36_PROT_SET` reader - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs36ProtSetR = crate::BitReader;
#[doc = "Field `PCS36_PROT_SET` writer - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs36ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS37_PROT_SET` reader - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs37ProtSetR = crate::BitReader;
#[doc = "Field `PCS37_PROT_SET` writer - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs37ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS38_PROT_SET` reader - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs38ProtSetR = crate::BitReader;
#[doc = "Field `PCS38_PROT_SET` writer - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs38ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS39_PROT_SET` reader - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs39ProtSetR = crate::BitReader;
#[doc = "Field `PCS39_PROT_SET` writer - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs39ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS40_PROT_SET` reader - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs40ProtSetR = crate::BitReader;
#[doc = "Field `PCS40_PROT_SET` writer - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs40ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS41_PROT_SET` reader - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs41ProtSetR = crate::BitReader;
#[doc = "Field `PCS41_PROT_SET` writer - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs41ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS42_PROT_SET` reader - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs42ProtSetR = crate::BitReader;
#[doc = "Field `PCS42_PROT_SET` writer - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs42ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS43_PROT_SET` reader - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs43ProtSetR = crate::BitReader;
#[doc = "Field `PCS43_PROT_SET` writer - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs43ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS44_PROT_SET` reader - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs44ProtSetR = crate::BitReader;
#[doc = "Field `PCS44_PROT_SET` writer - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs44ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS45_PROT_SET` reader - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs45ProtSetR = crate::BitReader;
#[doc = "Field `PCS45_PROT_SET` writer - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs45ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS46_PROT_SET` reader - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs46ProtSetR = crate::BitReader;
#[doc = "Field `PCS46_PROT_SET` writer - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs46ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS47_PROT_SET` reader - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs47ProtSetR = crate::BitReader;
#[doc = "Field `PCS47_PROT_SET` writer - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs47ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS48_PROT_SET` reader - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs48ProtSetR = crate::BitReader;
#[doc = "Field `PCS48_PROT_SET` writer - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs48ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS49_PROT_SET` reader - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs49ProtSetR = crate::BitReader;
#[doc = "Field `PCS49_PROT_SET` writer - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs49ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS50_PROT_SET` reader - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs50ProtSetR = crate::BitReader;
#[doc = "Field `PCS50_PROT_SET` writer - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs50ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS51_PROT_SET` reader - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs51ProtSetR = crate::BitReader;
#[doc = "Field `PCS51_PROT_SET` writer - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs51ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS52_PROT_SET` reader - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs52ProtSetR = crate::BitReader;
#[doc = "Field `PCS52_PROT_SET` writer - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs52ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS53_PROT_SET` reader - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs53ProtSetR = crate::BitReader;
#[doc = "Field `PCS53_PROT_SET` writer - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs53ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS54_PROT_SET` reader - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs54ProtSetR = crate::BitReader;
#[doc = "Field `PCS54_PROT_SET` writer - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs54ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS55_PROT_SET` reader - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs55ProtSetR = crate::BitReader;
#[doc = "Field `PCS55_PROT_SET` writer - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs55ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS56_PROT_SET` reader - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs56ProtSetR = crate::BitReader;
#[doc = "Field `PCS56_PROT_SET` writer - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs56ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS57_PROT_SET` reader - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs57ProtSetR = crate::BitReader;
#[doc = "Field `PCS57_PROT_SET` writer - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs57ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS58_PROT_SET` reader - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs58ProtSetR = crate::BitReader;
#[doc = "Field `PCS58_PROT_SET` writer - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs58ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS59_PROT_SET` reader - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs59ProtSetR = crate::BitReader;
#[doc = "Field `PCS59_PROT_SET` writer - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs59ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS60_PROT_SET` reader - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs60ProtSetR = crate::BitReader;
#[doc = "Field `PCS60_PROT_SET` writer - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs60ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS61_PROT_SET` reader - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs61ProtSetR = crate::BitReader;
#[doc = "Field `PCS61_PROT_SET` writer - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs61ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS62_PROT_SET` reader - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs62ProtSetR = crate::BitReader;
#[doc = "Field `PCS62_PROT_SET` writer - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs62ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS63_PROT_SET` reader - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs63ProtSetR = crate::BitReader;
#[doc = "Field `PCS63_PROT_SET` writer - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
pub type Pcs63ProtSetW<'a, REG> = crate::BitWriter<'a, REG>;
impl R {
    #[doc = "Bit 0 - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs32_prot_set(&self) -> Pcs32ProtSetR {
        Pcs32ProtSetR::new((self.bits & 1) != 0)
    }
    #[doc = "Bit 1 - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs33_prot_set(&self) -> Pcs33ProtSetR {
        Pcs33ProtSetR::new(((self.bits >> 1) & 1) != 0)
    }
    #[doc = "Bit 2 - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs34_prot_set(&self) -> Pcs34ProtSetR {
        Pcs34ProtSetR::new(((self.bits >> 2) & 1) != 0)
    }
    #[doc = "Bit 3 - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs35_prot_set(&self) -> Pcs35ProtSetR {
        Pcs35ProtSetR::new(((self.bits >> 3) & 1) != 0)
    }
    #[doc = "Bit 4 - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs36_prot_set(&self) -> Pcs36ProtSetR {
        Pcs36ProtSetR::new(((self.bits >> 4) & 1) != 0)
    }
    #[doc = "Bit 5 - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs37_prot_set(&self) -> Pcs37ProtSetR {
        Pcs37ProtSetR::new(((self.bits >> 5) & 1) != 0)
    }
    #[doc = "Bit 6 - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs38_prot_set(&self) -> Pcs38ProtSetR {
        Pcs38ProtSetR::new(((self.bits >> 6) & 1) != 0)
    }
    #[doc = "Bit 7 - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs39_prot_set(&self) -> Pcs39ProtSetR {
        Pcs39ProtSetR::new(((self.bits >> 7) & 1) != 0)
    }
    #[doc = "Bit 8 - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs40_prot_set(&self) -> Pcs40ProtSetR {
        Pcs40ProtSetR::new(((self.bits >> 8) & 1) != 0)
    }
    #[doc = "Bit 9 - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs41_prot_set(&self) -> Pcs41ProtSetR {
        Pcs41ProtSetR::new(((self.bits >> 9) & 1) != 0)
    }
    #[doc = "Bit 10 - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs42_prot_set(&self) -> Pcs42ProtSetR {
        Pcs42ProtSetR::new(((self.bits >> 10) & 1) != 0)
    }
    #[doc = "Bit 11 - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs43_prot_set(&self) -> Pcs43ProtSetR {
        Pcs43ProtSetR::new(((self.bits >> 11) & 1) != 0)
    }
    #[doc = "Bit 12 - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs44_prot_set(&self) -> Pcs44ProtSetR {
        Pcs44ProtSetR::new(((self.bits >> 12) & 1) != 0)
    }
    #[doc = "Bit 13 - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs45_prot_set(&self) -> Pcs45ProtSetR {
        Pcs45ProtSetR::new(((self.bits >> 13) & 1) != 0)
    }
    #[doc = "Bit 14 - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs46_prot_set(&self) -> Pcs46ProtSetR {
        Pcs46ProtSetR::new(((self.bits >> 14) & 1) != 0)
    }
    #[doc = "Bit 15 - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs47_prot_set(&self) -> Pcs47ProtSetR {
        Pcs47ProtSetR::new(((self.bits >> 15) & 1) != 0)
    }
    #[doc = "Bit 16 - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs48_prot_set(&self) -> Pcs48ProtSetR {
        Pcs48ProtSetR::new(((self.bits >> 16) & 1) != 0)
    }
    #[doc = "Bit 17 - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs49_prot_set(&self) -> Pcs49ProtSetR {
        Pcs49ProtSetR::new(((self.bits >> 17) & 1) != 0)
    }
    #[doc = "Bit 18 - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs50_prot_set(&self) -> Pcs50ProtSetR {
        Pcs50ProtSetR::new(((self.bits >> 18) & 1) != 0)
    }
    #[doc = "Bit 19 - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs51_prot_set(&self) -> Pcs51ProtSetR {
        Pcs51ProtSetR::new(((self.bits >> 19) & 1) != 0)
    }
    #[doc = "Bit 20 - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs52_prot_set(&self) -> Pcs52ProtSetR {
        Pcs52ProtSetR::new(((self.bits >> 20) & 1) != 0)
    }
    #[doc = "Bit 21 - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs53_prot_set(&self) -> Pcs53ProtSetR {
        Pcs53ProtSetR::new(((self.bits >> 21) & 1) != 0)
    }
    #[doc = "Bit 22 - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs54_prot_set(&self) -> Pcs54ProtSetR {
        Pcs54ProtSetR::new(((self.bits >> 22) & 1) != 0)
    }
    #[doc = "Bit 23 - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs55_prot_set(&self) -> Pcs55ProtSetR {
        Pcs55ProtSetR::new(((self.bits >> 23) & 1) != 0)
    }
    #[doc = "Bit 24 - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs56_prot_set(&self) -> Pcs56ProtSetR {
        Pcs56ProtSetR::new(((self.bits >> 24) & 1) != 0)
    }
    #[doc = "Bit 25 - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs57_prot_set(&self) -> Pcs57ProtSetR {
        Pcs57ProtSetR::new(((self.bits >> 25) & 1) != 0)
    }
    #[doc = "Bit 26 - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs58_prot_set(&self) -> Pcs58ProtSetR {
        Pcs58ProtSetR::new(((self.bits >> 26) & 1) != 0)
    }
    #[doc = "Bit 27 - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs59_prot_set(&self) -> Pcs59ProtSetR {
        Pcs59ProtSetR::new(((self.bits >> 27) & 1) != 0)
    }
    #[doc = "Bit 28 - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs60_prot_set(&self) -> Pcs60ProtSetR {
        Pcs60ProtSetR::new(((self.bits >> 28) & 1) != 0)
    }
    #[doc = "Bit 29 - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs61_prot_set(&self) -> Pcs61ProtSetR {
        Pcs61ProtSetR::new(((self.bits >> 29) & 1) != 0)
    }
    #[doc = "Bit 30 - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs62_prot_set(&self) -> Pcs62ProtSetR {
        Pcs62ProtSetR::new(((self.bits >> 30) & 1) != 0)
    }
    #[doc = "Bit 31 - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs63_prot_set(&self) -> Pcs63ProtSetR {
        Pcs63ProtSetR::new(((self.bits >> 31) & 1) != 0)
    }
}
impl W {
    #[doc = "Bit 0 - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs32_prot_set(&mut self) -> Pcs32ProtSetW<Pmprotset1Spec> {
        Pcs32ProtSetW::new(self, 0)
    }
    #[doc = "Bit 1 - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs33_prot_set(&mut self) -> Pcs33ProtSetW<Pmprotset1Spec> {
        Pcs33ProtSetW::new(self, 1)
    }
    #[doc = "Bit 2 - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs34_prot_set(&mut self) -> Pcs34ProtSetW<Pmprotset1Spec> {
        Pcs34ProtSetW::new(self, 2)
    }
    #[doc = "Bit 3 - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs35_prot_set(&mut self) -> Pcs35ProtSetW<Pmprotset1Spec> {
        Pcs35ProtSetW::new(self, 3)
    }
    #[doc = "Bit 4 - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs36_prot_set(&mut self) -> Pcs36ProtSetW<Pmprotset1Spec> {
        Pcs36ProtSetW::new(self, 4)
    }
    #[doc = "Bit 5 - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs37_prot_set(&mut self) -> Pcs37ProtSetW<Pmprotset1Spec> {
        Pcs37ProtSetW::new(self, 5)
    }
    #[doc = "Bit 6 - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs38_prot_set(&mut self) -> Pcs38ProtSetW<Pmprotset1Spec> {
        Pcs38ProtSetW::new(self, 6)
    }
    #[doc = "Bit 7 - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs39_prot_set(&mut self) -> Pcs39ProtSetW<Pmprotset1Spec> {
        Pcs39ProtSetW::new(self, 7)
    }
    #[doc = "Bit 8 - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs40_prot_set(&mut self) -> Pcs40ProtSetW<Pmprotset1Spec> {
        Pcs40ProtSetW::new(self, 8)
    }
    #[doc = "Bit 9 - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs41_prot_set(&mut self) -> Pcs41ProtSetW<Pmprotset1Spec> {
        Pcs41ProtSetW::new(self, 9)
    }
    #[doc = "Bit 10 - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs42_prot_set(&mut self) -> Pcs42ProtSetW<Pmprotset1Spec> {
        Pcs42ProtSetW::new(self, 10)
    }
    #[doc = "Bit 11 - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs43_prot_set(&mut self) -> Pcs43ProtSetW<Pmprotset1Spec> {
        Pcs43ProtSetW::new(self, 11)
    }
    #[doc = "Bit 12 - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs44_prot_set(&mut self) -> Pcs44ProtSetW<Pmprotset1Spec> {
        Pcs44ProtSetW::new(self, 12)
    }
    #[doc = "Bit 13 - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs45_prot_set(&mut self) -> Pcs45ProtSetW<Pmprotset1Spec> {
        Pcs45ProtSetW::new(self, 13)
    }
    #[doc = "Bit 14 - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs46_prot_set(&mut self) -> Pcs46ProtSetW<Pmprotset1Spec> {
        Pcs46ProtSetW::new(self, 14)
    }
    #[doc = "Bit 15 - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs47_prot_set(&mut self) -> Pcs47ProtSetW<Pmprotset1Spec> {
        Pcs47ProtSetW::new(self, 15)
    }
    #[doc = "Bit 16 - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs48_prot_set(&mut self) -> Pcs48ProtSetW<Pmprotset1Spec> {
        Pcs48ProtSetW::new(self, 16)
    }
    #[doc = "Bit 17 - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs49_prot_set(&mut self) -> Pcs49ProtSetW<Pmprotset1Spec> {
        Pcs49ProtSetW::new(self, 17)
    }
    #[doc = "Bit 18 - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs50_prot_set(&mut self) -> Pcs50ProtSetW<Pmprotset1Spec> {
        Pcs50ProtSetW::new(self, 18)
    }
    #[doc = "Bit 19 - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs51_prot_set(&mut self) -> Pcs51ProtSetW<Pmprotset1Spec> {
        Pcs51ProtSetW::new(self, 19)
    }
    #[doc = "Bit 20 - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs52_prot_set(&mut self) -> Pcs52ProtSetW<Pmprotset1Spec> {
        Pcs52ProtSetW::new(self, 20)
    }
    #[doc = "Bit 21 - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs53_prot_set(&mut self) -> Pcs53ProtSetW<Pmprotset1Spec> {
        Pcs53ProtSetW::new(self, 21)
    }
    #[doc = "Bit 22 - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs54_prot_set(&mut self) -> Pcs54ProtSetW<Pmprotset1Spec> {
        Pcs54ProtSetW::new(self, 22)
    }
    #[doc = "Bit 23 - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs55_prot_set(&mut self) -> Pcs55ProtSetW<Pmprotset1Spec> {
        Pcs55ProtSetW::new(self, 23)
    }
    #[doc = "Bit 24 - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs56_prot_set(&mut self) -> Pcs56ProtSetW<Pmprotset1Spec> {
        Pcs56ProtSetW::new(self, 24)
    }
    #[doc = "Bit 25 - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs57_prot_set(&mut self) -> Pcs57ProtSetW<Pmprotset1Spec> {
        Pcs57ProtSetW::new(self, 25)
    }
    #[doc = "Bit 26 - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs58_prot_set(&mut self) -> Pcs58ProtSetW<Pmprotset1Spec> {
        Pcs58ProtSetW::new(self, 26)
    }
    #[doc = "Bit 27 - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs59_prot_set(&mut self) -> Pcs59ProtSetW<Pmprotset1Spec> {
        Pcs59ProtSetW::new(self, 27)
    }
    #[doc = "Bit 28 - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs60_prot_set(&mut self) -> Pcs60ProtSetW<Pmprotset1Spec> {
        Pcs60ProtSetW::new(self, 28)
    }
    #[doc = "Bit 29 - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs61_prot_set(&mut self) -> Pcs61ProtSetW<Pmprotset1Spec> {
        Pcs61ProtSetW::new(self, 29)
    }
    #[doc = "Bit 30 - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs62_prot_set(&mut self) -> Pcs62ProtSetW<Pmprotset1Spec> {
        Pcs62ProtSetW::new(self, 30)
    }
    #[doc = "Bit 31 - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Sets the corresponding bit in PMPROTSET1 and PMPROTCLR1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs63_prot_set(&mut self) -> Pcs63ProtSetW<Pmprotset1Spec> {
        Pcs63ProtSetW::new(self, 31)
    }
}
#[doc = "Set-only register to protect PCS frames 32 to 63\n\nYou can [`read`](crate::Reg::read) this register and get [`pmprotset1::R`](R). You can [`reset`](crate::Reg::reset), [`write`](crate::Reg::write), [`write_with_zero`](crate::Reg::write_with_zero) this register using [`pmprotset1::W`](W). You can also [`modify`](crate::Reg::modify) this register. See [API](https://docs.rs/svd2rust/#read--modify--write-api)."]
pub struct Pmprotset1Spec;
impl crate::RegisterSpec for Pmprotset1Spec {
    type Ux = u32;
}
#[doc = "`read()` method returns [`pmprotset1::R`](R) reader structure"]
impl crate::Readable for Pmprotset1Spec {}
#[doc = "`write(|w| ..)` method takes [`pmprotset1::W`](W) writer structure"]
impl crate::Writable for Pmprotset1Spec {
    type Safety = crate::Unsafe;
    const ZERO_TO_MODIFY_FIELDS_BITMAP: u32 = 0;
    const ONE_TO_MODIFY_FIELDS_BITMAP: u32 = 0;
}
#[doc = "`reset()` method sets PMPROTSET1 to value 0"]
impl crate::Resettable for Pmprotset1Spec {
    const RESET_VALUE: u32 = 0;
}
