#[doc = "Register `PMPROTCLR1` reader"]
pub type R = crate::R<Pmprotclr1Spec>;
#[doc = "Register `PMPROTCLR1` writer"]
pub type W = crate::W<Pmprotclr1Spec>;
#[doc = "Field `PCS32_PROT_CLR` reader - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs32ProtClrR = crate::BitReader;
#[doc = "Field `PCS32_PROT_CLR` writer - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs32ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS33_PROT_CLR` reader - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs33ProtClrR = crate::BitReader;
#[doc = "Field `PCS33_PROT_CLR` writer - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs33ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS34_PROT_CLR` reader - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs34ProtClrR = crate::BitReader;
#[doc = "Field `PCS34_PROT_CLR` writer - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs34ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS35_PROT_CLR` reader - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs35ProtClrR = crate::BitReader;
#[doc = "Field `PCS35_PROT_CLR` writer - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs35ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS36_PROT_CLR` reader - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs36ProtClrR = crate::BitReader;
#[doc = "Field `PCS36_PROT_CLR` writer - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs36ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS37_PROT_CLR` reader - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs37ProtClrR = crate::BitReader;
#[doc = "Field `PCS37_PROT_CLR` writer - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs37ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS38_PROT_CLR` reader - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs38ProtClrR = crate::BitReader;
#[doc = "Field `PCS38_PROT_CLR` writer - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs38ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS39_PROT_CLR` reader - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs39ProtClrR = crate::BitReader;
#[doc = "Field `PCS39_PROT_CLR` writer - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs39ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS40_PROT_CLR` reader - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs40ProtClrR = crate::BitReader;
#[doc = "Field `PCS40_PROT_CLR` writer - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs40ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS41_PROT_CLR` reader - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs41ProtClrR = crate::BitReader;
#[doc = "Field `PCS41_PROT_CLR` writer - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs41ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS42_PROT_CLR` reader - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs42ProtClrR = crate::BitReader;
#[doc = "Field `PCS42_PROT_CLR` writer - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs42ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS43_PROT_CLR` reader - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs43ProtClrR = crate::BitReader;
#[doc = "Field `PCS43_PROT_CLR` writer - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs43ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS44_PROT_CLR` reader - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs44ProtClrR = crate::BitReader;
#[doc = "Field `PCS44_PROT_CLR` writer - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs44ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS45_PROT_CLR` reader - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs45ProtClrR = crate::BitReader;
#[doc = "Field `PCS45_PROT_CLR` writer - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs45ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS46_PROT_CLR` reader - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs46ProtClrR = crate::BitReader;
#[doc = "Field `PCS46_PROT_CLR` writer - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs46ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS47_PROT_CLR` reader - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs47ProtClrR = crate::BitReader;
#[doc = "Field `PCS47_PROT_CLR` writer - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs47ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS48_PROT_CLR` reader - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs48ProtClrR = crate::BitReader;
#[doc = "Field `PCS48_PROT_CLR` writer - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs48ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS49_PROT_CLR` reader - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs49ProtClrR = crate::BitReader;
#[doc = "Field `PCS49_PROT_CLR` writer - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs49ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS50_PROT_CLR` reader - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs50ProtClrR = crate::BitReader;
#[doc = "Field `PCS50_PROT_CLR` writer - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs50ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS51_PROT_CLR` reader - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs51ProtClrR = crate::BitReader;
#[doc = "Field `PCS51_PROT_CLR` writer - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs51ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS52_PROT_CLR` reader - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs52ProtClrR = crate::BitReader;
#[doc = "Field `PCS52_PROT_CLR` writer - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs52ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS53_PROT_CLR` reader - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs53ProtClrR = crate::BitReader;
#[doc = "Field `PCS53_PROT_CLR` writer - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs53ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS54_PROT_CLR` reader - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs54ProtClrR = crate::BitReader;
#[doc = "Field `PCS54_PROT_CLR` writer - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs54ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS55_PROT_CLR` reader - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs55ProtClrR = crate::BitReader;
#[doc = "Field `PCS55_PROT_CLR` writer - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs55ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS56_PROT_CLR` reader - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs56ProtClrR = crate::BitReader;
#[doc = "Field `PCS56_PROT_CLR` writer - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs56ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS57_PROT_CLR` reader - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs57ProtClrR = crate::BitReader;
#[doc = "Field `PCS57_PROT_CLR` writer - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs57ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS58_PROT_CLR` reader - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs58ProtClrR = crate::BitReader;
#[doc = "Field `PCS58_PROT_CLR` writer - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs58ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS59_PROT_CLR` reader - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs59ProtClrR = crate::BitReader;
#[doc = "Field `PCS59_PROT_CLR` writer - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs59ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS60_PROT_CLR` reader - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs60ProtClrR = crate::BitReader;
#[doc = "Field `PCS60_PROT_CLR` writer - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs60ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS61_PROT_CLR` reader - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs61ProtClrR = crate::BitReader;
#[doc = "Field `PCS61_PROT_CLR` writer - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs61ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS62_PROT_CLR` reader - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs62ProtClrR = crate::BitReader;
#[doc = "Field `PCS62_PROT_CLR` writer - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs62ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
#[doc = "Field `PCS63_PROT_CLR` reader - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs63ProtClrR = crate::BitReader;
#[doc = "Field `PCS63_PROT_CLR` writer - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
pub type Pcs63ProtClrW<'a, REG> = crate::BitWriter<'a, REG>;
impl R {
    #[doc = "Bit 0 - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs32_prot_clr(&self) -> Pcs32ProtClrR {
        Pcs32ProtClrR::new((self.bits & 1) != 0)
    }
    #[doc = "Bit 1 - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs33_prot_clr(&self) -> Pcs33ProtClrR {
        Pcs33ProtClrR::new(((self.bits >> 1) & 1) != 0)
    }
    #[doc = "Bit 2 - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs34_prot_clr(&self) -> Pcs34ProtClrR {
        Pcs34ProtClrR::new(((self.bits >> 2) & 1) != 0)
    }
    #[doc = "Bit 3 - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs35_prot_clr(&self) -> Pcs35ProtClrR {
        Pcs35ProtClrR::new(((self.bits >> 3) & 1) != 0)
    }
    #[doc = "Bit 4 - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs36_prot_clr(&self) -> Pcs36ProtClrR {
        Pcs36ProtClrR::new(((self.bits >> 4) & 1) != 0)
    }
    #[doc = "Bit 5 - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs37_prot_clr(&self) -> Pcs37ProtClrR {
        Pcs37ProtClrR::new(((self.bits >> 5) & 1) != 0)
    }
    #[doc = "Bit 6 - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs38_prot_clr(&self) -> Pcs38ProtClrR {
        Pcs38ProtClrR::new(((self.bits >> 6) & 1) != 0)
    }
    #[doc = "Bit 7 - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs39_prot_clr(&self) -> Pcs39ProtClrR {
        Pcs39ProtClrR::new(((self.bits >> 7) & 1) != 0)
    }
    #[doc = "Bit 8 - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs40_prot_clr(&self) -> Pcs40ProtClrR {
        Pcs40ProtClrR::new(((self.bits >> 8) & 1) != 0)
    }
    #[doc = "Bit 9 - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs41_prot_clr(&self) -> Pcs41ProtClrR {
        Pcs41ProtClrR::new(((self.bits >> 9) & 1) != 0)
    }
    #[doc = "Bit 10 - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs42_prot_clr(&self) -> Pcs42ProtClrR {
        Pcs42ProtClrR::new(((self.bits >> 10) & 1) != 0)
    }
    #[doc = "Bit 11 - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs43_prot_clr(&self) -> Pcs43ProtClrR {
        Pcs43ProtClrR::new(((self.bits >> 11) & 1) != 0)
    }
    #[doc = "Bit 12 - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs44_prot_clr(&self) -> Pcs44ProtClrR {
        Pcs44ProtClrR::new(((self.bits >> 12) & 1) != 0)
    }
    #[doc = "Bit 13 - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs45_prot_clr(&self) -> Pcs45ProtClrR {
        Pcs45ProtClrR::new(((self.bits >> 13) & 1) != 0)
    }
    #[doc = "Bit 14 - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs46_prot_clr(&self) -> Pcs46ProtClrR {
        Pcs46ProtClrR::new(((self.bits >> 14) & 1) != 0)
    }
    #[doc = "Bit 15 - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs47_prot_clr(&self) -> Pcs47ProtClrR {
        Pcs47ProtClrR::new(((self.bits >> 15) & 1) != 0)
    }
    #[doc = "Bit 16 - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs48_prot_clr(&self) -> Pcs48ProtClrR {
        Pcs48ProtClrR::new(((self.bits >> 16) & 1) != 0)
    }
    #[doc = "Bit 17 - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs49_prot_clr(&self) -> Pcs49ProtClrR {
        Pcs49ProtClrR::new(((self.bits >> 17) & 1) != 0)
    }
    #[doc = "Bit 18 - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs50_prot_clr(&self) -> Pcs50ProtClrR {
        Pcs50ProtClrR::new(((self.bits >> 18) & 1) != 0)
    }
    #[doc = "Bit 19 - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs51_prot_clr(&self) -> Pcs51ProtClrR {
        Pcs51ProtClrR::new(((self.bits >> 19) & 1) != 0)
    }
    #[doc = "Bit 20 - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs52_prot_clr(&self) -> Pcs52ProtClrR {
        Pcs52ProtClrR::new(((self.bits >> 20) & 1) != 0)
    }
    #[doc = "Bit 21 - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs53_prot_clr(&self) -> Pcs53ProtClrR {
        Pcs53ProtClrR::new(((self.bits >> 21) & 1) != 0)
    }
    #[doc = "Bit 22 - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs54_prot_clr(&self) -> Pcs54ProtClrR {
        Pcs54ProtClrR::new(((self.bits >> 22) & 1) != 0)
    }
    #[doc = "Bit 23 - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs55_prot_clr(&self) -> Pcs55ProtClrR {
        Pcs55ProtClrR::new(((self.bits >> 23) & 1) != 0)
    }
    #[doc = "Bit 24 - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs56_prot_clr(&self) -> Pcs56ProtClrR {
        Pcs56ProtClrR::new(((self.bits >> 24) & 1) != 0)
    }
    #[doc = "Bit 25 - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs57_prot_clr(&self) -> Pcs57ProtClrR {
        Pcs57ProtClrR::new(((self.bits >> 25) & 1) != 0)
    }
    #[doc = "Bit 26 - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs58_prot_clr(&self) -> Pcs58ProtClrR {
        Pcs58ProtClrR::new(((self.bits >> 26) & 1) != 0)
    }
    #[doc = "Bit 27 - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs59_prot_clr(&self) -> Pcs59ProtClrR {
        Pcs59ProtClrR::new(((self.bits >> 27) & 1) != 0)
    }
    #[doc = "Bit 28 - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs60_prot_clr(&self) -> Pcs60ProtClrR {
        Pcs60ProtClrR::new(((self.bits >> 28) & 1) != 0)
    }
    #[doc = "Bit 29 - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs61_prot_clr(&self) -> Pcs61ProtClrR {
        Pcs61ProtClrR::new(((self.bits >> 29) & 1) != 0)
    }
    #[doc = "Bit 30 - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs62_prot_clr(&self) -> Pcs62ProtClrR {
        Pcs62ProtClrR::new(((self.bits >> 30) & 1) != 0)
    }
    #[doc = "Bit 31 - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    pub fn pcs63_prot_clr(&self) -> Pcs63ProtClrR {
        Pcs63ProtClrR::new(((self.bits >> 31) & 1) != 0)
    }
}
impl W {
    #[doc = "Bit 0 - 0:0\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs32_prot_clr(&mut self) -> Pcs32ProtClrW<Pmprotclr1Spec> {
        Pcs32ProtClrW::new(self, 0)
    }
    #[doc = "Bit 1 - 1:1\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs33_prot_clr(&mut self) -> Pcs33ProtClrW<Pmprotclr1Spec> {
        Pcs33ProtClrW::new(self, 1)
    }
    #[doc = "Bit 2 - 2:2\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs34_prot_clr(&mut self) -> Pcs34ProtClrW<Pmprotclr1Spec> {
        Pcs34ProtClrW::new(self, 2)
    }
    #[doc = "Bit 3 - 3:3\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs35_prot_clr(&mut self) -> Pcs35ProtClrW<Pmprotclr1Spec> {
        Pcs35ProtClrW::new(self, 3)
    }
    #[doc = "Bit 4 - 4:4\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs36_prot_clr(&mut self) -> Pcs36ProtClrW<Pmprotclr1Spec> {
        Pcs36ProtClrW::new(self, 4)
    }
    #[doc = "Bit 5 - 5:5\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs37_prot_clr(&mut self) -> Pcs37ProtClrW<Pmprotclr1Spec> {
        Pcs37ProtClrW::new(self, 5)
    }
    #[doc = "Bit 6 - 6:6\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs38_prot_clr(&mut self) -> Pcs38ProtClrW<Pmprotclr1Spec> {
        Pcs38ProtClrW::new(self, 6)
    }
    #[doc = "Bit 7 - 7:7\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs39_prot_clr(&mut self) -> Pcs39ProtClrW<Pmprotclr1Spec> {
        Pcs39ProtClrW::new(self, 7)
    }
    #[doc = "Bit 8 - 8:8\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs40_prot_clr(&mut self) -> Pcs40ProtClrW<Pmprotclr1Spec> {
        Pcs40ProtClrW::new(self, 8)
    }
    #[doc = "Bit 9 - 9:9\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs41_prot_clr(&mut self) -> Pcs41ProtClrW<Pmprotclr1Spec> {
        Pcs41ProtClrW::new(self, 9)
    }
    #[doc = "Bit 10 - 10:10\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs42_prot_clr(&mut self) -> Pcs42ProtClrW<Pmprotclr1Spec> {
        Pcs42ProtClrW::new(self, 10)
    }
    #[doc = "Bit 11 - 11:11\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs43_prot_clr(&mut self) -> Pcs43ProtClrW<Pmprotclr1Spec> {
        Pcs43ProtClrW::new(self, 11)
    }
    #[doc = "Bit 12 - 12:12\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs44_prot_clr(&mut self) -> Pcs44ProtClrW<Pmprotclr1Spec> {
        Pcs44ProtClrW::new(self, 12)
    }
    #[doc = "Bit 13 - 13:13\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs45_prot_clr(&mut self) -> Pcs45ProtClrW<Pmprotclr1Spec> {
        Pcs45ProtClrW::new(self, 13)
    }
    #[doc = "Bit 14 - 14:14\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs46_prot_clr(&mut self) -> Pcs46ProtClrW<Pmprotclr1Spec> {
        Pcs46ProtClrW::new(self, 14)
    }
    #[doc = "Bit 15 - 15:15\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs47_prot_clr(&mut self) -> Pcs47ProtClrW<Pmprotclr1Spec> {
        Pcs47ProtClrW::new(self, 15)
    }
    #[doc = "Bit 16 - 16:16\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs48_prot_clr(&mut self) -> Pcs48ProtClrW<Pmprotclr1Spec> {
        Pcs48ProtClrW::new(self, 16)
    }
    #[doc = "Bit 17 - 17:17\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs49_prot_clr(&mut self) -> Pcs49ProtClrW<Pmprotclr1Spec> {
        Pcs49ProtClrW::new(self, 17)
    }
    #[doc = "Bit 18 - 18:18\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs50_prot_clr(&mut self) -> Pcs50ProtClrW<Pmprotclr1Spec> {
        Pcs50ProtClrW::new(self, 18)
    }
    #[doc = "Bit 19 - 19:19\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs51_prot_clr(&mut self) -> Pcs51ProtClrW<Pmprotclr1Spec> {
        Pcs51ProtClrW::new(self, 19)
    }
    #[doc = "Bit 20 - 20:20\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs52_prot_clr(&mut self) -> Pcs52ProtClrW<Pmprotclr1Spec> {
        Pcs52ProtClrW::new(self, 20)
    }
    #[doc = "Bit 21 - 21:21\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs53_prot_clr(&mut self) -> Pcs53ProtClrW<Pmprotclr1Spec> {
        Pcs53ProtClrW::new(self, 21)
    }
    #[doc = "Bit 22 - 22:22\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs54_prot_clr(&mut self) -> Pcs54ProtClrW<Pmprotclr1Spec> {
        Pcs54ProtClrW::new(self, 22)
    }
    #[doc = "Bit 23 - 23:23\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs55_prot_clr(&mut self) -> Pcs55ProtClrW<Pmprotclr1Spec> {
        Pcs55ProtClrW::new(self, 23)
    }
    #[doc = "Bit 24 - 24:24\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs56_prot_clr(&mut self) -> Pcs56ProtClrW<Pmprotclr1Spec> {
        Pcs56ProtClrW::new(self, 24)
    }
    #[doc = "Bit 25 - 25:25\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs57_prot_clr(&mut self) -> Pcs57ProtClrW<Pmprotclr1Spec> {
        Pcs57ProtClrW::new(self, 25)
    }
    #[doc = "Bit 26 - 26:26\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs58_prot_clr(&mut self) -> Pcs58ProtClrW<Pmprotclr1Spec> {
        Pcs58ProtClrW::new(self, 26)
    }
    #[doc = "Bit 27 - 27:27\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs59_prot_clr(&mut self) -> Pcs59ProtClrW<Pmprotclr1Spec> {
        Pcs59ProtClrW::new(self, 27)
    }
    #[doc = "Bit 28 - 28:28\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs60_prot_clr(&mut self) -> Pcs60ProtClrW<Pmprotclr1Spec> {
        Pcs60ProtClrW::new(self, 28)
    }
    #[doc = "Bit 29 - 29:29\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs61_prot_clr(&mut self) -> Pcs61ProtClrW<Pmprotclr1Spec> {
        Pcs61ProtClrW::new(self, 29)
    }
    #[doc = "Bit 30 - 30:30\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs62_prot_clr(&mut self) -> Pcs62ProtClrW<Pmprotclr1Spec> {
        Pcs62ProtClrW::new(self, 30)
    }
    #[doc = "Bit 31 - 31:31\\]
Readable in user and privileged modes 1 = The corresponding peripheral memory frame can be written to only in privileged mode but can be read in both user and privileged modes. 0 = The corresponding peripheral memory frame can be written to and read from in both user and privileged modes. Writable only in privileged mode 1 = Clears the corresponding bit in PMPROTCLR1 and PMPROTSET1 registers 0 = Has no effect"]
    #[inline(always)]
    #[must_use]
    pub fn pcs63_prot_clr(&mut self) -> Pcs63ProtClrW<Pmprotclr1Spec> {
        Pcs63ProtClrW::new(self, 31)
    }
}
#[doc = "Clear-only register to protect PCS frames 32 to 63\n\nYou can [`read`](crate::Reg::read) this register and get [`pmprotclr1::R`](R). You can [`reset`](crate::Reg::reset), [`write`](crate::Reg::write), [`write_with_zero`](crate::Reg::write_with_zero) this register using [`pmprotclr1::W`](W). You can also [`modify`](crate::Reg::modify) this register. See [API](https://docs.rs/svd2rust/#read--modify--write-api)."]
pub struct Pmprotclr1Spec;
impl crate::RegisterSpec for Pmprotclr1Spec {
    type Ux = u32;
}
#[doc = "`read()` method returns [`pmprotclr1::R`](R) reader structure"]
impl crate::Readable for Pmprotclr1Spec {}
#[doc = "`write(|w| ..)` method takes [`pmprotclr1::W`](W) writer structure"]
impl crate::Writable for Pmprotclr1Spec {
    type Safety = crate::Unsafe;
    const ZERO_TO_MODIFY_FIELDS_BITMAP: u32 = 0;
    const ONE_TO_MODIFY_FIELDS_BITMAP: u32 = 0;
}
#[doc = "`reset()` method sets PMPROTCLR1 to value 0"]
impl crate::Resettable for Pmprotclr1Spec {
    const RESET_VALUE: u32 = 0;
}
